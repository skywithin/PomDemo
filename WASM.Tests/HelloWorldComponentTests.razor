@code
{
    [Fact]
    public void HelloWorldComponentRendersCorrectly()
    {
		using var ctx = new TestContext();

		var cut = ctx.RenderComponent<HelloWorldComponent>();

        cut.MarkupMatches(@<h1>Hello, world from Blazor</h1>);
    }
}
